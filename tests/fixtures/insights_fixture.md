# Insights (fixture)

A synthetic Phase 4 test fixture — **NOT** a production artifact. Stands in
for `artifacts/crew1/insights.md` (a Phase 6+ EDA & Insights Analyst
deliverable, which does not exist yet). Family A only checks that this file
**exists** and is **non-empty** — its content is never parsed or read by
`contract/validator.py`.

Placeholder content so `tests/unit/test_validator_artifacts.py` has a real,
non-empty file on disk to check the existence/non-emptiness of.
