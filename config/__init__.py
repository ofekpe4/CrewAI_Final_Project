"""Project configuration package.

Holds ``settings.yaml`` (non-secret tunables) and ``llm.py`` (the single source of
truth for LLM configuration). Importing anything here has no side effects and
never requires a secret.
"""
