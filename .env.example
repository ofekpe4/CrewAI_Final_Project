# Copy this file to .env and fill in real values.
# .env is gitignored and must never be committed. This file holds NAMES ONLY — no values.

# LLM provider (Plan §B.2: OpenAI).
# Required only from Phase 6 onward; Phases 0–5 run without it.
OPENAI_API_KEY=
