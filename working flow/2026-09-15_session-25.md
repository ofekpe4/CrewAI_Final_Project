# Session 25 — 2026-09-15

## Session Goal

**FINAL PHASE 6 QUALITY VERIFICATION — not Phase 7.** Session 24 fixed the
EDA-report chart issue completely but its one authorized corrective live
run hit a NEW critical halt while attempting to fix Contract Architect
feature curation. This session's explicit, narrower goal: fix ONLY the
missing column-name **grounding** rule (every name the Architect writes
must come from the actual `profile_clean_dataset` output — never a
guessed/canonical/remembered spelling), add an explicit two-case
identifier rule (present → hard-exclude; absent → do not mention
anywhere), extend the offline prompt-contract test accordingly, and spend
exactly ONE more authorized live Crew 1 run to verify it end-to-end. STOP
on any critical failure — no further live run without fresh approval.

## Work Completed

### 0 — Preflight

Rehydrated from `PROJECT_PLAN.md`, Sessions 23–24's records, current
`config/tasks.yaml`, current Analyst Crew code, `data/README.md`, the
`ContractDraft` schema/guardrail, and the current artifacts on disk.
Confirmed on branch `fix/phase-6-quality-closeout` (PR #11, still open,
unmerged from Session 24): Session 24's EDA fix present (3 figures in
`eda_report.html`); Phase 6 mocked tests green (56/56 before this
session's changes); gate on the current restored artifacts PASS (0
errors, 0 warnings); `.env` ignored; `OPENAI_API_KEY available: YES`
(never printed); no Phase 7 implementation.

### 1 — The grounding-rule fix (`config/tasks.yaml`, prompt-only)

Added a new CRITICAL paragraph, ahead of the existing four-bucket feature-
curation guidance, stating explicitly: every column name written anywhere
in the draft (`columns`, `required_features`, `excluded_features`,
`primary_key`) must appear character-for-character in THIS run's actual
`profile_clean_dataset` output — never reconstructed from memory, a
"canonical" snake_case convention, the raw Telco source names, or a prior
example.

Rewrote the identifier bucket as an explicit two-case rule:
- **Case A** (identifier present): use its exact real name, hard-exclude
  it (`exclusion_type: "identifier"`, `enforcement: "hard"`), never in
  `required_features` — unchanged in substance from Session 24, now
  explicitly a "case."
- **Case B** (identifier absent — e.g. the Inspector `drop_column`'d it,
  Session 24's exact real-run scenario): do NOT invent or recreate it
  anywhere — not in `columns`, `required_features`, `excluded_features`,
  or `primary_key`.

Added explicit `primary_key` guidance for Case B (the schema requires a
non-empty `primary_key.columns`, so simply saying "don't mention it"
isn't sufficient on its own): pick the best actually-present column(s),
and be honest — `unique.value: false` with a justification — rather than
fabricate an identifier that does not exist. No deterministic code
touched (`plans/contract_draft.py`, `contract/builder.py`, every
guardrail/callback all unchanged).

### 2 — Prompt-contract test extension

Added 5 new assertions to
`test_contract_architect_prompt_covers_feature_curation` (still testing
the ACTUAL loaded task text, never a canned LLM answer): the
character-for-character grounding rule text is present; the "never
reconstruct/guess/assume" sentence is present; both "Case A"/"Case B"
labels are present; Case B's "do NOT invent or recreate" text is present;
the honest-non-unique `primary_key` guidance is present. Total Phase 6
mocked suite: 61/61 (was 56).

### 3 — The one authorized live run

Ran `scripts/run_crew1.py` once. Outcome: **`status=halted_agent_failure`
at Task 3, after exhausting 2 retries (3 total attempts).**

- Task 1 (Inspector): kept the identifier this time, renamed
  `customerID` → `customer_id` (Case A for this run).
- Task 2 (Analyst): succeeded — real insights, evidence keys all real
  (the Session 23 fix continues to hold).
- Task 3 (Architect): **failed the guardrail's structural coverage check**
  (the Session 23 `clean_columns` extension — proven, again, to be doing
  real protective work): `"ContractDraft column coverage does not match
  clean_data.csv: clean_data.csv columns with no draft declaration:
  ['MonthlyCharges', 'TotalCharges', 'tenure']; draft declares columns
  absent from clean_data.csv: ['monthly_charges', 'tenure_months',
  'total_charges']"`.

**Root cause, precisely:** the Architect correctly grounded the
identifier this run (used the real `customer_id`, exactly matching this
run's actual data — the Session 24/25 fix DID work for that specific
column). But for three OTHER columns that this run's Inspector agent left
in raw casing (`MonthlyCharges`, `TotalCharges`, `tenure`), the Architect
reverted to habitual "canonical" spellings
(`monthly_charges`/`total_charges`/`tenure_months`) instead of the real
names — exactly the general grounding-rule violation this session's
prompt fix targeted, but which the model applied inconsistently: fully
internalized for the identifier (which the prompt calls out with special,
repeated emphasis and two labeled cases), only partially internalized for
ordinary columns (which get one general sentence at the top of the
section). A real, informative finding: the identifier-specific emphasis
worked; the general rule needs the same kind of repeated, concrete
emphasis to reliably generalize to every column, not just the one most
heavily highlighted.

**Per this session's explicit instruction ("If it fails critically, STOP.
Do not perform another live run without fresh user approval"), no further
live run was attempted.**

### 4 — Reconciling artifacts after the halt

Run 4 had already overwritten `clean_data.csv`/`eda_report.html`/
`insights.md` with its own (Task 1/2-successful, Task 3-orphaned) data and
deleted `dataset_contract.json` before halting. Restored all four tracked
artifacts to the last known-good, gate-passing state (`git checkout --`,
identical technique to Session 24 §6), discarded run 4's untracked debris,
re-verified SHA256 integrity match, and re-ran the Phase 4 gate:
`passed=True`, 0 errors, 0 warnings — unchanged from before this session.

## Files Modified

- `src/harbor_vale/crews/analyst_crew/config/tasks.yaml` — grounding rule
  + two-case identifier handling + `primary_key` honesty guidance (§1).
- `tests/unit/test_analyst_crew.py` — 5 new prompt-contract assertions
  (§2).

No artifact files changed from their Session 24, already-committed,
already-gate-passing state (restored after the live run's halt, §4).

## Architectural Decisions

1. **The grounding-rule fix stayed prompt-only, exactly as instructed.**
   *Why:* explicit instruction; and the failure this session's live run
   surfaced is itself proof the deterministic layer (the guardrail's
   `clean_columns` coverage check, added Session 23) is the correct,
   necessary backstop for exactly this class of mistake — no code change
   was indicated, only a prompt-quality gap.
2. **No second live run, despite the failure being narrower/closer than
   Session 24's** (one correctly-grounded column, three not). *Why:* the
   instruction was unconditional; a "the fix mostly worked" judgment call
   is exactly the kind of self-granted exception the instruction exists to
   prevent.
3. **Artifacts reverted to the last known-good state rather than left
   halted or hand-patched** — identical reasoning to Session 24 §6:
   keeps every committed artifact internally consistent (SHA256 match,
   gate PASS) without fabricating anything or leaving the repo in a
   broken state.

## Commands / Tests Run

| Command | Result |
|---|---|
| `python tests/unit/test_analyst_crew.py` (before the live run) | 61/61 **all passed** |
| Full Phase 0–5 regression (24 files) | **all passed** |
| `python -m pip check` | `No broken requirements found.` |
| `git rev-parse v0.3.0-gate^{commit}` | `26c8d87...` — unchanged |
| `python scripts/run_crew1.py` (the one authorized live run) | `status=halted_agent_failure` at Task 3, 3 attempts — real cause diagnosed (§3) |
| Post-halt SHA256 + gate re-check on restored artifacts | match confirmed; `passed=True`, 0 errors, 0 warnings |
| Full regression + mocked suite re-run after restore | **all passed** (24 Phase 0–5 files + 61 Phase 6 checks) |

## Results

**The EDA fix (Session 24) remains fully verified and unaffected.** The
feature-curation/grounding fix is **further improved and more heavily
tested**, and this session's live run proved it is a genuine partial
improvement (the identifier case, the most heavily emphasized rule, was
followed correctly) — but did **not** achieve a full, live, end-to-end
PASS: three ordinary columns still got canonicalized names instead of
their real ones, tripping the very guardrail this project built to catch
exactly that. Per the explicit instruction, this session stopped after
one live attempt rather than iterating further. The committed
`dataset_contract.json` is unchanged from Session 24 (Known Issue #1 from
that session's record remains open). Phase 6's underlying architecture
required no change across any of Sessions 23, 24, or 25.

## Git Status

- Still on `fix/phase-6-quality-closeout` (PR #11, open since Session 24),
  no new branch created (per this session's "use the existing Phase 6
  branch/fix workflow" instruction).
- Files intended for this session's commit: the two files under "Files
  Modified" above, plus this record.
- `v0.3.0-gate` unchanged at `26c8d87`. No new tag.
- Commit hash: reported in this session's **final response**, not
  restated here.
- **Not merged** — per this session's own framing ("if and only if all
  Phase 6 quality criteria pass: commit, push, PR/merge"), and this run's
  criteria did not fully pass, PR #11 stays open for a human merge
  decision, same posture as Session 24 ended in.

## Known Issues

1. **`dataset_contract.json`'s feature curation is still weak** (carried
   over from Sessions 23–24, NOT resolved by this session's live attempt
   either): `excluded_features` empty; `customerID` in `required_features`
   undifferentiated (this is the ALREADY-COMMITTED artifact from Session
   23/24 — this session's run 4, which WOULD have had correct identifier
   handling, never reached a successful Task 3 completion). A future
   iteration should give the "ground every column name, not just the
   identifier" rule the same kind of concrete, repeated, example-driven
   emphasis this session gave the identifier case specifically — e.g. a
   short worked example showing a case where the Inspector left a column
   in raw casing and the correct Architect behavior is to use that exact
   raw name.
2. `TotalCharges`/equivalent was still never marked `target_leakage` in
   any run so far — remains true, not a new issue.
3. `pytest` remains intentionally absent from the project `.venv`.

## Next Session

**Phase 7 may begin only with a fresh, explicit approval** — not granted
by this session. Recommend, before Phase 7's Feature Engineer agent is
built: one more live Crew 1 iteration (fresh approval required) using the
strengthened general-grounding-rule emphasis suggested in Known Issue #1
above, OR accept the current curation as a documented limitation Phase 7's
Feature Engineer agent must specifically account for (e.g. its own prompt
explicitly warns it that `required_features` may include a non-predictive
identifier and it must apply its own judgment rather than trust the list
blindly).

## Important Context

- **Total live Crew 1 runs across Sessions 23, 24, and 25: 4** — 2 in
  Session 23, 1 in Session 24, 1 in this session, each separately and
  explicitly authorized by the user's own prompts, each one honestly
  reported regardless of outcome.
- **The `clean_columns` coverage guardrail (Session 23) has now correctly
  caught a real mismatch in a live run** (this session, §3) — concrete
  evidence, not just a unit-test claim, that this specific deterministic
  safeguard earns its place in the architecture.
- **A pattern worth naming for whoever attempts the next iteration**: this
  project's live runs show the Inspector agent's column-naming choices are
  genuinely non-deterministic across runs (fully snake_case, fully raw, or
  — this session — a mix). Any future Crew 1 or Crew 2 prompt that
  references a "the cleaned column for X" must either read it from a tool
  at the time or be phrased to require exactly that, the same way this
  session's grounding rule now does explicitly for the Contract Architect.
