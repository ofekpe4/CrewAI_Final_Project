"""Harbor & Vale — industry-simulated AI product workflow.

Package root. Intentionally free of side effects: importing ``harbor_vale`` or any
submodule must not touch the network, call an LLM, require an API key, create
Agents/Crews, read raw data, or write artifacts. See PROJECT_PLAN.md.
"""
